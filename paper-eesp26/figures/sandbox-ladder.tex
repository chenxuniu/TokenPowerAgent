\begin{figure*}[t]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  x=1cm,y=1cm,
  every node/.style={font=\sffamily},
  box/.style={draw=tpGray,rounded corners=1.2pt,minimum height=0.94cm,
    text width=2.02cm,align=center,inner sep=2.6pt,font=\scriptsize},
  lane/.style={draw=tpGray,dashed,rounded corners=2pt,inner xsep=5pt,
    inner ysep=3.5pt},
  flow/.style={-{Stealth[length=2.1mm]},line width=0.8pt,draw=tpGray},
  feedback/.style={-{Stealth[length=2.1mm]},line width=0.8pt,draw=tpTealDark}
]
  \node[box,fill=tpGrayLight,text width=1.9cm] (scenario) at (0,0)
    {\textbf{Frozen Contract}\\workload / config\\model, SLO, target};

  \node[box,fill=tpTealLight] (project) at (3.05,0.58)
    {\textbf{CPU Projection}\\analytical + residual};
  \node[box,fill=tpGoldLight] (gate) at (5.82,0.58)
    {\textbf{Scope Gate}\\controls + workload};
  \node[box,fill=tpGrayLight] (freeze) at (8.59,0.58)
    {\textbf{Freeze + Rank}\\interval + SHA-256};

  \node[box,fill=tpBlueLight] (anchor) at (3.05,-0.58)
    {\textbf{Sparse H100 Probe}\\anchor + development};
  \node[box,fill=tpBlueLight] (verify) at (8.59,-0.58)
    {\textbf{Blind H100 Run}\\selected workload + DCGM};

  \node[box,fill=tpRoseLight,text width=2.15cm] (release) at (11.65,0)
    {\textbf{Evidence Decision}\\verify or abstain};

  \node[lane,fit=(project)(gate)(freeze)] (cpulane) {};
  \node[lane,fit=(anchor)(verify)] (gpulane) {};
  \node[font=\tiny\bfseries,text=tpTealDark,anchor=south west]
    at (cpulane.north west) {CPU path: screen and gate, zero incremental GPU-hours};
  \node[font=\tiny\bfseries,text=tpRoseDark,anchor=north west]
    at (gpulane.south west) {GPU path: sparse calibration and validation};

  \draw[flow] (scenario.north east) -- (project.west);
  \draw[flow] (scenario.south east) -- (anchor.west);
  \draw[feedback] (anchor.north) -- (project.south);
  \draw[flow] (project) -- (gate);
  \draw[flow] (gate) -- node[above,font=\tiny]{pass} (freeze);
  \draw[flow] (freeze.south) -- (verify.north);
  \draw[flow] (verify.east) -- (release.south west);
\end{tikzpicture}%
}
\caption{\system{} separates HPC resource paths.  CPU-resident projections
screen candidates without reserving a target GPU; selected H100 runs calibrate
and validate them.  Prediction becomes evidence only through the release gate.}
\label{fig:sandbox-ladder}
\end{figure*}
