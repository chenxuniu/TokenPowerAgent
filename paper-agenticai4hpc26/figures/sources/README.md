# Editable Figure Sources

This directory contains the editable SVG sources for the six selected
publication figures. The corresponding PDFs used by LaTeX live in
`../results/`.

Use the operational stage names `Sandbox`, `Probe`, and `Verify` in visible
figure text. Legacy `L0/L1/L4` values belong only to archived evidence schemas
and must not be presented as a four-stage hierarchy.
